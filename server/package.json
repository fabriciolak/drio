{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.3",
    "typescript": "^5",
    "shared": "workspace:*"
  },
  "private": true,
  "dependencies": {
    "express": "^5.1.0"
  }
}
